name: Hello World Workflow

on:
  push:
    branches:
      - main

jobs:
  say_hello:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: echo "🎉 Hello from GitHub Actions!"
